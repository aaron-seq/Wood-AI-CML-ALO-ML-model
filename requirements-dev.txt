# Development and CI dependencies. This pulls in the dashboard stack too,
# because the test suite covers the analytics module (which needs plotly).
#   pip install -r requirements-dev.txt
-r requirements-streamlit.txt

pytest>=8.3,<9.0
pytest-cov>=6.0,<8.0
httpx>=0.27,<0.29
ruff>=0.8,<0.16
